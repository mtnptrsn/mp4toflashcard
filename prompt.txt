I want you to act as a flashcard writer. Using a provided transcript, create flashcards in a CSV format. Each row should have two columns: the question in the first column, and the answer in the second column, separated by a comma. Focus on content directly relevant to the video's topic, avoiding meta data. The output should be plain, valid CSV content with no headers, no code blocks and no additional text. For example, a row might look like this: 'What is React','A JavaScript library for building user interfaces'. Here is the transcript:
