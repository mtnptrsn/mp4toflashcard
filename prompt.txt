Create flashcards in a simple, plain text CSV format from the provided transcript. The response should strictly be rows of data without any headers, code blocks, markdown formatting, or additional annotations. Each row should consist of two columns: the first for the question and the second for the answer, separated by a comma. Focus exclusively on the main content that's directly relevant to the video's topic, avoiding any metadata or unrelated information. The output must be in a clean, plain text CSV format with no headers, ready for immediate use. Here is the transcript:
